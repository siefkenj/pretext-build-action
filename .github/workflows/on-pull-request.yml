name: Test Action

on:
    push:
        branches: ["master", "main"]
    pull_request:
        branches: ["master", "main"]

jobs:
    build:
        runs-on: ubuntu-latest

        steps:
            - name: Checkout code
              uses: actions/checkout@v4
              with:
                  repository: "https://github.com/PreTeXtBook/pretext.git"

            - name: Run action
              uses: siefkenj/pretext-build-action@main
